# ADR-007：V2 失败分类模型

日期：2026-07-15  
状态：已接受

## 决策

Domain 持久化稳定 `FailureCode`、诊断消息、建议动作和 UTC 时间。GUI 与报告根据失败码生成本地化文本；不得把未实现的软件路径误报为硬件不支持。

## 后果

- 任务、证据报告和界面共享一致的失败语义。
- `runtime_not_implemented`、`sdk_missing`、`hardware_unsupported`、`artifact_incompatible` 等可被准确区分。
