# ADR-004：V2 不可变产物与原子提交

日期：2026-07-15  
状态：已接受

## 决策

产物必须先在 staging 写入、校验文件清单和哈希，再原子提交为可用 Artifact。数据库记录与产物可见性必须在同一用例事务内完成；失败、取消和崩溃不能留下可使用的半成品。

## 后果

- 后续 Artifact Store 是 Dataset、Model、Report 的唯一可交付出口。
- 不允许把旧输出目录、临时 ONNX 或未验证报告直接注册为模型产物。
