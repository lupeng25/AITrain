# ADR-001：V2 破坏性重构策略

日期：2026-07-15  
状态：已接受

## 决策

项目尚未上线，V2 不兼容 V1 SQLite、Worker 协议、内部 C++ API、任务状态和报告字段。检测到 V1 数据库时必须明确拒绝，并提示删除重建；不得增加双写或兼容读取。

## 后果

- 重构可以删除历史迁移和状态包袱。
- 开发环境的数据库、临时产物和测试数据需要重建。
- 每个 V2 替换切片必须有独立端到端测试，不能靠 V1 回退掩盖缺口。
