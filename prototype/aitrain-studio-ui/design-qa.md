# AITrain Studio HTML 预览设计 QA

## 对比目标

- 视觉真值：`E:\code\AITrain\prototype\aitrain-studio-ui\references\option-1.png`
- 实现截图：`E:\code\AITrain\prototype\aitrain-studio-ui\qa\training-1440x1024.png`
- 全画面对照：`E:\code\AITrain\prototype\aitrain-studio-ui\qa\comparison-final.png`
- 重点区域对照：`E:\code\AITrain\prototype\aitrain-studio-ui\qa\comparison-focused.png`
- 视口：1440 × 1024
- 状态：轴承缺陷检测项目 / 训练实验 / Epoch 18 of 50 / 指标曲线 / 右侧检查器展开 / 中文

## Findings

当前没有未解决的 P0、P1 或 P2 问题。

- 字体与排版：使用 Microsoft YaHei UI / Segoe UI，与方案 1 的 Windows 原生桌面气质一致；标题、分组标题、标签和技术字段形成清晰层级。重点区域原始像素对照确认没有标题换行、标签拥挤或控件裁切。
- 间距与布局节奏：侧栏、顶部状态栏、四步流程、左侧配置、中央运行区和右侧检查器保持相同的信息架构。8px 网格、6–8px 圆角和克制边框贯穿全部页面。
- 色彩与视觉令牌：深海军蓝导航、白色工作面、蓝色主操作、绿色健康状态和琥珀色警告与方案 1 一致；未使用渐变或玻璃效果。
- 图像质量与资产：应用图标复用生产资源；轴承样本、缺陷样本和推理 overlay 均为真实栅格资产，清晰度、构图和工业检测语境一致。界面图标全部来自 Fluent UI 图标体系，没有手绘 SVG 或字符图标替代。
- 文案与内容：页面内容使用真实感的轴承检测 mock 数据，技术名称、路径和日志保持原文；中英文切换覆盖导航、标题、状态、表单及主要操作。
- 交互状态：导航、页签、语言切换、检查器折叠、训练停止确认、数据校验加载/成功、任务筛选、模型对比、导出和推理运行均有可见状态。
- 响应式：1280 × 800 时侧栏折叠为 72px 图标栏，检查器为 272px；1440 × 1024 为 200px / 272px；1920 × 1080 为 216px / 304px。三个视口均无 body 或工作区横向溢出，主要操作可见。
- 可访问性：语义按钮、页签角色、状态提示、图片替代文本和清晰焦点样式已提供；应用限定为 1280px 以上桌面视口。

## Comparison History

### 第 1 轮

- [P2] 1440 高度下指标区偏短，训练主体下方留白明显；与方案 1 的纵向密度不一致。
- [P2] 运行摘要缺少方案 1 中可见的 mAP50-95 核心指标。
- 修复：将 `.chart-large` 改为随视口高度增长的 `clamp()` 高度；运行摘要从四列扩展到五列并增加 mAP50-95 = 0.576。
- 修复后证据：`qa/comparison-final.png` 与 `qa/comparison-focused.png`。训练主体在 1440 × 1024 中充分使用可用高度，五项指标保持单行且无挤压。

### 第 2 轮

- 重新检查完整画面与训练主体原始像素对照，未发现新的 P0、P1、P2。
- 方案图同时展示日志区，而实现按已确认计划将指标、日志和 checkpoint 组织为可交互页签；这是明确的交互结构差异，不影响信息覆盖或核心任务，归类为可接受约束。

## 浏览器验证

- 1280 × 800：无横向滚动；72px 导航折叠生效；停止训练主操作可见。
- 1440 × 1024：无横向滚动；200px 导航、272px 检查器和训练主体比例通过。
- 1920 × 1080：无横向滚动；216px 导航、304px 检查器和自适应主区通过。
- 已实际验证训练日志页签与中英文切换；自动化测试覆盖导航、页签、训练状态、数据校验、任务筛选与检查器折叠。
- 浏览器控制台 error / warning：0。

## Follow-up Polish

- [P3] 后续若希望进一步贴近方案图，可为曲线增加 Loss 辅助轴和悬停标记；当前 Recharts 曲线已满足本阶段训练指标预览需求。

## Implementation Checklist

- [x] 方案 1 主结构、令牌与密度
- [x] 9 个一级工作区及全部嵌套页签
- [x] 核心演示交互和确认弹窗
- [x] 中英文切换
- [x] 1280 / 1440 / 1920 三视口
- [x] 全画面与重点区域同屏对照
- [x] 控制台与自动化测试

final result: passed
