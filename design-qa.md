# AITrain Studio Qt 界面设计验收

## 验收对象

- 设计基准：`prototype/aitrain-studio-ui/qa/training-1280x800.png`
- Qt 修正版：`artifacts/ui-layout-audit/post-fix/04-training-windows.png`
- 同画面对比：`artifacts/ui-layout-audit/post-fix/comparison-training-reference-vs-qt.png`，左侧为 HTML 基准，右侧为 Qt 修正版。
- 全界面证据：`artifacts/ui-layout-audit/post-fix/` 下 9 个一级工作区及部署高级参数展开态截图。
- 视口：1280 × 800，Windows 平台真实字体渲染。
- 状态：HTML 基准为模拟训练运行态；Qt 截图未打开真实项目，按产品边界显示空闲态。

## 同画面对比

| 区域 | 结果 | 说明 |
| --- | --- | --- |
| 应用框架 | 通过 | 深海军蓝紧凑导航、顶部项目状态栏、浅色工作面和右侧检查器入口保持统一。 |
| 页面上下文 | 通过 | 面包屑、标题、项目状态和工作区工具条层级与 HTML 基准一致。 |
| 工作流 | 通过 | 数据准备、训练实验、评估与模型、部署验证四步流程完整，当前阶段高亮明确。 |
| 训练运行头 | 通过 | 实验名、后端摘要和主要操作采用紧凑单行布局。 |
| 主工作区 | 通过 | 配置与监控主次清楚；1280px 下检查器按响应式规则默认折叠，避免挤压主任务。 |
| 响应式 | 通过 | 1280px 下导航自动收为 72px 图标栏，无横向滚动和静默裁切；检查器可手动展开。 |
| 视觉系统 | 通过 | 白色工作面、蓝色主操作、绿色健康状态、统一边框、圆角和 8px 间距节奏延续方案 1。 |
| 全界面布局 | 通过 | 总览、项目、数据集、任务、模型、部署、环境和系统设置均完成 1280px 首屏复核。 |

## 修正迭代

1. 修正 1280px 检查器策略：1366px 以下默认折叠，同时尊重用户手动开关。
2. 数据集复核改为 320px 左栏与自适应队列，按钮改为 2 × 2，消除单字竖排。
3. 任务列表与详情降低固定最小宽度，检查器打开和关闭均无右侧裁切。
4. 部署高级参数改为默认折叠的完整标签表单，并加入纵向滚动容器。
5. 总览移除空表横向滚动和强制等高空白；项目与模型库清除重复主操作。
6. 训练配置栏扩大并允许文本换行、控件收缩；系统能力工具条恢复内容高度。
7. 使用 Windows 平台重新生成逐页截图，修复自动截图中发现的部署控件二次裁切。

## 允许差异

- HTML 基准展示真实感模拟运行数据；Qt 实现不伪造项目、GPU 占用、训练曲线或完成指标，真实数据由项目与 Worker 事件填充。
- HTML 基准在 1280px 展示检查器；Qt 修正版按既定响应式规则默认折叠，用户可从顶栏手动打开。这是为消除任务页和表单页裁切而保留的产品差异。
- Qt 5.12 使用平台可用标准图标，细节与 Fluent 图标存在 P3 级差异，不影响布局与操作。

final result: passed
